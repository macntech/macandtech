{{!< default}}


    {{!-- Posts--}}
    <div class="row mt-2 p-3">
        
        {{!-- 
        <div class="row">
            <div class="col-auto"><h2>Writing</h2></div>
            <div class="col"></div>
        </div>--}}
        
        {{#foreach posts}}
            {{> "post-card"}}
        {{/foreach}}
        
        {{!-- 
        <div class="row mt-4">
            <a href="archive" class="nodecoration">Archiv <i class="far fa-save"></i></a>
        </div>--}}

    </div>

    {{> "tag-cloud"}}
